import argparse

if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="Train ACT baseline placeholder.")
    parser.add_argument("--epochs", type=int, default=1, help="Number of epochs")
    args = parser.parse_args()
    print(f"Train ACT placeholder: epochs={args.epochs}")
